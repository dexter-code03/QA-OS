## QA Platform Frontend (Local)

### Run

```bash
cd platform/frontend
npm install
npm run dev
```

Vite dev server: `http://127.0.0.1:5173`

### Backend

The dev server proxies API and WebSocket to `http://127.0.0.1:9000` (see `vite.config.ts`).

